package bitwise.devices.usb.drivers.ptp.types;

import bitwise.devices.usb.drivers.ptp.types.prim.UInt32;

public class StorageID extends UInt32 {
	public StorageID(int in_value) {
		super(in_value);
	}
}
